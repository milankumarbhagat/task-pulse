import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.milankumar.taskpulse',
  appName: 'Task Pulse',
  webDir: 'dist/task-pulse/browser'
};

export default config;
